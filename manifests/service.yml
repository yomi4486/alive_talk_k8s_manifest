apiVersion: v1
kind: Service
metadata:
  name: progate-lion-hackathon-service
  namespace: default
spec:
  selector:
    app: progate-lion-hackathon-app
  ports:
    - protocol: TCP
      port: 443
      targetPort: 3000
  type: LoadBalancer
