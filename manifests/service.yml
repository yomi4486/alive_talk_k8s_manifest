apiVersion: v1
Kind: Service
metadata:
  name: progate-lion-hackathon-service
  namespace: default
spec:
  selector:
    app: progate-lion-hackathon-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
